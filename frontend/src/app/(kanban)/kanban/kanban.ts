import { Component } from '@angular/core';
import { Row } from '../row/row';

@Component({
  selector: 'app-kanban',
  imports: [Row],
  templateUrl: './kanban.html',
  styleUrl: './kanban.css'
})
export class Kanban {

}
